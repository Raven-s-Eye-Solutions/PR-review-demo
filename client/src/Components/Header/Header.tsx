import React, { FunctionComponent } from 'react';

export const Header: FunctionComponent = () => {
  return (
    <header>
      <h1>Welcome to the calulator!</h1>
    </header>
  )
}

export default Header;
